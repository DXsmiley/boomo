doctype html
html(lang="en")
    head
        link(rel="stylesheet", type="text/css", href="/static/style.css")
    body
        div(class="fillpage blue")
            div(class="center")
                h1 Play Boom-O!
                p
                    span(class="roomy") Name:
                    input(type="text", id="name-input")
                p(class="subtle") Alpha-numeric characters only
